---
pool: 
  name: Azure Pipeline
  Vmimage: 'ubuntu-latest'
trigger:
  - main
steps:
  - script: sudo apt update
  - script: sudo apt install openjdk-11-jdk -y
  - script: sudo apt install maven -y
  - script: cd openmrs-core && mvn clean package